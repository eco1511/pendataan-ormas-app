"use client";import{useEffect,useState}from'react';import{Search,RefreshCw}from'lucide-react';
export default function LogsClient(){const[data,setData]=useState<any[]>([]);const[pag,setPag]=useState<any>({page:1,total:0,totalPages:0});const[q,setQ]=useState('');const[loading,setLoading]=useState(false);const load=async(page=1)=>{setLoading(true);const r=await fetch('/api/log-aktivitas?'+new URLSearchParams({page:String(page),search:q}));const d=await r.json();if(d.success){setData(d.data||[]);setPag(d)}setLoading(false)};useEffect(()=>{const t=setTimeout(()=>load(1),300);return()=>clearTimeout(t)},[q]);return <div className="space-y-4"><div className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex gap-3"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-gray-400" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full rounded-xl border py-2.5 pl-10 pr-3" placeholder="Cari log (Username, Aktivitas, Keterangan)..."/></div><button onClick={()=>load(pag.page)} className="rounded-xl border px-4"><RefreshCw size={17}/></button></div></div><div className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-gray-50"><tr>{['Waktu','Username','Aktivitas','ID Data','Keterangan'].map(x=><th key={x} className="px-5 py-3 text-left">{x}</th>)}</tr></thead><tbody>{loading?<tr><td colSpan={5} className="py-10 text-center text-gray-400">Memuat...</td></tr>:data.length?data.map((d:any)=><tr key={d._id} className="border-t hover:bg-gray-50"><td className="whitespace-nowrap px-5 py-3 text-xs">{new Date(d.waktu).toLocaleString('id-ID')}</td><td className="px-5 py-3 font-medium">{d.username}</td><td className="px-5 py-3"><span className="rounded-full bg-gray-100 px-2 py-1 text-xs">{d.aktivitas}</span></td><td className="px-5 py-3 text-xs text-gray-500">{d.idData}</td><td className="px-5 py-3">{d.keterangan}</td></tr>):<tr><td colSpan={5} className="py-10 text-center text-gray-400">Tidak ada log</td></tr>}</tbody></table></div><div className="flex items-center justify-between border-t bg-gray-50 px-5 py-3 text-sm"><span>{pag.total} data</span><div className="flex items-center gap-2"><button disabled={pag.page<=1} onClick={()=>load(pag.page-1)} className="rounded-lg border bg-white px-3 py-1.5 disabled:opacity-40">‹</button><span>Hal {pag.page}/{pag.totalPages||1}</span><button disabled={pag.page>=pag.totalPages} onClick={()=>load(pag.page+1)} className="rounded-lg border bg-white px-3 py-1.5 disabled:opacity-40">›</button></div></div></div></div>}
